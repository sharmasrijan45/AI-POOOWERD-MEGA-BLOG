import React from 'react'

export default function Container({children}) {
  return 
    <div className='w-full max-w-7xl mx-auto px-4'>
      {children}
    </div>;
  
}
//commonn concept in almost every framework that we make a container only for designing 